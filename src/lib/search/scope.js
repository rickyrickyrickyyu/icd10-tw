/**
 * 皮膚科子集（首載範圍）的結果不可信 → 應改查全部 CM。網頁與 CLI 共用這一條規則。
 *
 * 規則：沒結果，或第一名不是主題對應（ATM）。
 * ★ v13 以前用「非 ATM 且分數 < 5」：分數門檻完全擋不住 —— 子集裡沒有心臟科的碼，
 *   heart failure 會以 14.59 分命中 A52.06（心血管梅毒），三叉神經痛 32.97 分命中 B02.22。
 *   實測第 1 名正確率（皮膚科範圍送出查詢後的最終結果）：
 *
 *              舊規則   新規則   永遠全庫   永遠皮膚科
 *   clinical   97.3     98.2     98.2      96.9
 *   general    61.3     96.1     96.7      42.0
 *   diverse    55.0     97.2     97.8      34.4
 *   fresh      61.3    100      100        38.7
 *
 *   新規則沒有讓任何一題皮膚科題變差；剩下與「永遠全庫」的差距是子集裡的錯誤 ATM
 *   （peptic ulcer → L88、mumps → L12.1）。
 */
export function isWeak(res) {
  const top = res?.items?.[0];
  return !top || !top.atm;
}
